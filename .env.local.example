ADMIN_PASSWORD=your-secret-password

# Cloudflare R2 — only needed in production (configured via wrangler.jsonc bindings)
# For local development, data is stored in data/ and public/uploads/ directories.
# R2_PUBLIC_URL=https://pub-{hash}.r2.dev  (or your custom domain)
